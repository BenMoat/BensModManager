@model PaginatedList<BensModManager.Models.Mod>

<div class="title-container text-center text-light">
    <h1 class="mods-header offcanvas-header text-light">Mods</h1>
</div>

<form asp-action="Index" method="get">
    <div class="container" id="inlineSearch">
        <div class="row justify-content-md-center">
            <div class="col col-md-4">
                <input type="text"
                       name="ModName"
                       id="searchModName"
                       class="form-control bg-dark text-light"
                       placeholder="Mod Name"
                       value="@ViewData["ModName"]" />
            </div>

            <div class="col col-md-3">
                <select for="ModType"
                        id="searchModType"
                        name="ModType"
                        class="form-select bg-dark text-light"
                        value="@ViewData["ModType"]">
                </select>
                <option selected disabled value=""></option>
            </div>

            <div class="col col-lg-1">
                <button type="submit"
                        id="searchBtn"
                        class="form-control btn btn-success text-white"
                        value="Search">
                    <i class="fas fa-search"></i>
                </button>
            </div>

            <div class="col col-md-2">
                <a onclick="modPopup('@Url.Action("AddOrEdit","Mods",null,Context.Request.Scheme)','New Mod')"
                   class="form-control btn btn-success text-white">New Mod</a>
            </div>

            @if (@ViewData["ModName"] != null || @ViewData["ModType"] != null)
            {
                <div class="col col-md-2">
                    <a class="form-control btn btn-info text-white"
                   asp-action="Index">
                        Back to Full List
                    </a>
                </div>
            }
        </div>
    </div>
</form>

<form asp-action="ModTypes" method="get">
</form>
<div id="tableAJAX">
    <table class="table table-dark table-striped table-hover">
        <thead>
            <tr id="headers" class="align-middle">
                <th scope="col" class="col-7">
                    <a asp-action="Index" asp-route-sortOrder="@ViewData["ModNameSortParam"]" asp-route-modName="@ViewData["ModName"]" asp-route-modType="@ViewData["ModType"]">Mod Name</a>
                </th>
                <th scope="col" class="col-2">
                    <a asp-action="Index" asp-route-sortOrder="@ViewData["PriceSortParam"]" asp-route-modName="@ViewData["ModName"]" asp-route-modType="@ViewData["ModType"]">Price</a>
                </th>
                <th scope="col" class="col-3">
                    <a asp-action="Index" asp-route-sortOrder="@ViewData["ModTypeSortParam"]" asp-route-modName="@ViewData["ModName"]" asp-route-modType="@ViewData["ModType"]">Mod Type</a>
                </th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr onclick="modPopup('@Url.Action("AddOrEdit","Mods",new {id=item.ID},Context.Request.Scheme)','Update Mod')"
                    class="focusAnchor">
                    <td>
                        @Html.DisplayFor(modelItem => item.ModName)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Price)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.ModType)
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>

@if (!Model.Any())
{
    <h2 class="text-light">No Mods Found</h2>
}
else
{
    <h1 id="totalSum" class="text-light">Total: @string.Format(new System.Globalization.CultureInfo("en-GB"), "{0:C}", @Model.Sum(x => x.Price))</h1>
}

@{
    var prevDisabled = !Model.HasPreviousPage ? "disabled" : "";
    var nextDisabled = !Model.HasNextPage ? "disabled" : "";
}

<div class="container align-middle text-center">
    <a asp-action="Index"
       asp-route-sortOrder="@ViewData["CurrentSort"]"
       asp-route-pageNumber="@(Model.PageIndex - 1)"
       asp-route-currentFilter="@ViewData["CurrentFilter"]"
       class="btn btn-default btn-outline-light @prevDisabled">
        Previous
    </a>
    <a asp-action="Index"
       asp-route-sortOrder="@ViewData["CurrentSort"]"
       asp-route-pageNumber="@(Model.PageIndex + 1)"
       asp-route-currentFilter="@ViewData["CurrentFilter"]"
       class="btn btn-default btn-outline-light @nextDisabled">
        Next
    </a>
</div>